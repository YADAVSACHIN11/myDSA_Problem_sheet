void reverseArray(vector<int> &arr , int m) {
    vector<int>ans;
    for(int i=0;i<=m;i++){
        ans.push_back(arr[i]);
    }
    for(int i=arr.size()-1;i>m;i--){
        ans.push_back(arr[i]);
    }
    arr = ans;      	
}
