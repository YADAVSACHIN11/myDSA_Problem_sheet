#include <bits/stdc++.h> 
void insertionSort(int n, vector<int> &arr){
    for(int k=1;k<n;k++){
        for(int j=k;j>=0;j--){
          if (arr[j] < arr[j - 1]) {
            swap(arr[j], arr[j - 1]);
          }
        }
    }
}
