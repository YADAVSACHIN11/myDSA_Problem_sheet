class Solution {
  public:
    int countFreq(vector<int>& arr, int target) {
        int count=0;
        for(int v:arr){
            if(v==target){
                count++;
            }
        }
        return count;
    }
};
